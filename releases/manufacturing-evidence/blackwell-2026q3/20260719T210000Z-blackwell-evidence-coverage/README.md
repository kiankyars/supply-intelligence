# blackwell-manufacturing-evidence-coverage-2026-07-19

As of `2026-07-19`. Recorded `2026-07-19T21:00:00Z`.

**This is an evidence-coverage audit of a synthetic manufacturing run. It does not estimate actual Blackwell output and applies no source-input replacements.**

Open `dashboard.html` first. `coverage.json` is the complete machine-readable audit; `input_coverage.csv` preserves the ranked synthetic inputs; `claim_assessments.csv` separates constraint decisions from nonbinding evidence; and `sources/` freezes every selected claim snapshot and source manufacturing artifact used by this release.
