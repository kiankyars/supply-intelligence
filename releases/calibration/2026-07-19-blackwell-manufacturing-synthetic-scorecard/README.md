# blackwell-manufacturing-synthetic-scorecard-2026-07-19

As of: `2026-07-19`. Scored outcomes: `6`. Frozen forecast vintages: `1`.

**This checked scorecard uses synthetic outcomes. It validates the calibration contract and is not evidence of historical forecast accuracy.**

Open `dashboard.html` first. `result.json` preserves every score, grouping, proposal, warning, and evidence record. `dataset.json` and `sources/forecasts/` retain exact inputs; `replay-dataset.json` changes only their relative paths so the scorecard can be recomputed with this release as `source_root`. Calibration proposals are never auto-applied.
