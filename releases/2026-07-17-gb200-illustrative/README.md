# GB200 NVL72 illustrative chain reconciliation

Quarter: `2026-Q3`. As of: `2026-07-17`. Monte Carlo draws: `20,000`.

**This is an illustrative scenario. Synthetic capacity, yield, allocation, economics, and consensus inputs are not market estimates.**

Open `dashboard.html` first. `result.json` is the complete machine-readable output; `input_estimates.csv` and `evidence.csv` provide the audit trail.
