# micron-fy2026q3-guidance-backtest

Micron Technology `FY2026-Q3`. As of `2026-07-19`. Metrics scored: `7`.

**This is an evidence-backed reconstruction of external company guidance, not a native AI Supply Intelligence forecast. It is ineligible for model calibration.**

Open `dashboard.html` first. `result.json` and `scores.csv` retain the range-coverage and midpoint-error audit. `case.json` and `sources/` retain the exact normalized observations and hashes. Management ranges are not treated as probability quantiles.
