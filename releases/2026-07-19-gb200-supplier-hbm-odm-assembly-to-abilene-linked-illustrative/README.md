# GB200 supplier-HBM, ODM-assembly, and Abilene chain demonstrator

Quarter: `2026-Q3`. As of: `2026-07-19`. Monte Carlo draws: `20,000`.

**This is a linked illustrative run. Frozen manufacturing, system-assembly, and site-operational results, the site-allocation share, and remaining chain constraints contain synthetic inputs. It is not a global production estimate or an estimate of actual shipments or deployments.**

Open `dashboard.html` first. `scenario.json` is the generated linked scenario. The base scenario, link recipe, and all three source results are preserved byte for byte; `link_lineage.json` records every replacement, coverage guard, and quantile-to-triangular distribution mapping.
