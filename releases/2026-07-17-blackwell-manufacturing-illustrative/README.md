# Blackwell wafer, HBM, and package conversion

Quarter: `2026-Q3`. As of: `2026-07-17`. Monte Carlo draws: `20,000`.

**This is an illustrative manufacturing run. Wafer starts, die geometry, yields, binning, supplier allocation, and package capacity are synthetic. The output is not an estimate of actual Blackwell production.**

Open `dashboard.html` first. `result.json` contains the complete conversion and audit payload. `conversion_outputs.csv` separates physical outputs from `input_estimates.csv`; `evidence_gaps.csv` ranks synthetic inputs for replacement; and `evidence.csv` preserves source lineage.
