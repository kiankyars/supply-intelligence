# Blackwell wafer, HBM, and package conversion

Revision: `blackwell-wafer-format-2026-07-19`. Quarter: `2026-Q3`. As of: `2026-07-19`.

This release replaced `1` synthetic input with an exact-scope, hash-pinned claim. `20` modeled inputs remain synthetic, so the scenario remains illustrative.

Open `dashboard.html` first. `revision.json` records each before/after estimate and whether its numeric value changed. `lineage/` retains the exact predecessor, target catalog, claim snapshots, and selections needed to audit the revision.
