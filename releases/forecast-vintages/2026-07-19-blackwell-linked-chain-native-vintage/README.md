# blackwell-linked-chain-native-vintage-2026q3

As of: `2026-07-19`. Recorded: `2026-07-20T06:10:00Z`. Forecasts: `5`. Raw draws: `20,000`.

**This is a genuine pre-outcome native-model vintage, but its checked source scenario is synthetic and illustrative. It is not an estimate of actual production, shipments, installation, or operation and is ineligible for evidence-backed model calibration.**

Open `dashboard.html` first. `registry.json` preserves the exact original recipe; `sources/source-release/` preserves the source manifest, result, and complete raw draw ledger byte for byte. `replay-registry.json` changes only the source-release path so this release can validate and rebuild in isolation. No outcome or score is present. A later outcome must pass the frozen scope and evidence contract.
